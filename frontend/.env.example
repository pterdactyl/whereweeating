# Copy to frontend/.env — see README.md for full setup.

# Local dev: same origin as Vite so /api is proxied to the backend (port 5000).
# Production: do not set this variable.
VITE_API_BASE_URL=http://localhost:5173
