# Local dev only: API base URL. Use http://localhost:5173 so Vite proxies /api to the backend.
# Production (Vercel): leave VITE_API_BASE_URL unset — the app uses the same origin as the page.
VITE_API_BASE_URL=http://localhost:5173
